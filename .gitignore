node_modules/
dist/
dist-ssr/
# Native web-asset build (VITE_TARGET=native). Copied into the Capacitor
# projects by `npx cap sync`, so it is generated output like `dist/`.
dist-native/
build.log
*.local
.DS_Store
coverage/
.vite/
.eslintcache
*.tsbuildinfo

# Local env overrides. The Firebase web config is PUBLIC by design and is
# committed in src/lib/firebase.ts — nothing secret belongs here.
.env
.env.local
.env.*.local
